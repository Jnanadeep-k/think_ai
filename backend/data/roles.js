// This is our "list of allowed roles" for now.
// Later, when a real database is connected, this can move to a Roles table.
const roles = ["Admin", "Instructor", "TA", "Learner"];

module.exports = { roles };
